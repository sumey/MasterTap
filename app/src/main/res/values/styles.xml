<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light"/>

    <style name="ActionBar" parent="Widget.AppCompat.Light.ActionBar">
        <item name="background">@android:color/holo_orange_light</item>
    </style>

    <style name="ProgressBar.Horizontal" parent="android:Widget.ProgressBar.Horizontal">
        <item name="android:minHeight">20dip</item>
        <item name="android:maxHeight">20dip</item>
    </style>

    <style name="PagerTabStrip">
        <item name="background">@android:color/holo_red_dark</item>
    </style>

</resources>
